<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Store 배포용 (TestFlight 포함) -->
    <key>method</key>
    <string>app-store</string>

    <!-- 팀 ID: GitHub Secret APPLE_TEAM_ID 와 동일한 값으로 설정 -->
    <key>teamID</key>
    <string>$(APPLE_TEAM_ID)</string>

    <!-- 앱 Bundle ID → Provisioning Profile 매핑 -->
    <key>provisioningProfiles</key>
    <dict>
        <key>com.dreamnewspaper.app</key>
        <string>꿈신문사 App Store</string>
    </dict>

    <!-- 서명 옵션 -->
    <key>signingStyle</key>
    <string>manual</string>

    <key>signingCertificate</key>
    <string>iPhone Distribution</string>

    <!-- 심볼 업로드 (크래시 리포트 분석용) -->
    <key>uploadSymbols</key>
    <true/>

    <!-- Bitcode 비활성화 (Capacitor 앱) -->
    <key>compileBitcode</key>
    <false/>

    <!-- App Store Connect 자동 업로드 비활성화 (xcrun altool로 별도 처리) -->
    <key>destination</key>
    <string>export</string>
</dict>
</plist>
